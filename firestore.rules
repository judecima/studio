rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // MODO ABIERTO: Permite total lectura y escritura sin autenticación
      allow read, write: if true;
    }
  }
}
